"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.EscrowExtension = void 0;
const byte_utils_1 = require("@1inch/byte-utils");
const fusion_sdk_1 = require("@1inch/fusion-sdk");
const assert_1 = __importDefault(require("assert"));
const address_complement_js_1 = require("../../domains/addresses/address-complement.js");
const index_js_1 = require("../../domains/auction-details/index.js");
const index_js_2 = require("../../domains/hash-lock/index.js");
const index_js_3 = require("../../domains/time-locks/index.js");
const index_js_4 = require("../../domains/addresses/index.js");
const coder_js_1 = require("../../utils/coder.js");
/**
 * Extension for cross-chain escrow orders.
 */
class EscrowExtension extends fusion_sdk_1.FusionExtension {
    hashLockInfo;
    dstChainId;
    dstToken;
    srcSafetyDeposit;
    dstSafetyDeposit;
    timeLocks;
    dstAddressFirstPart;
    static CROSS_CHAIN_DATA_TYPES = [
        index_js_2.HashLock.Web3Type,
        'uint256', // dst chain id
        'uint256', // dst token
        'uint256', // src/dst safety deposit
        index_js_3.TimeLocks.Web3Type
    ];
    /** Cross-chain data is 160 bytes */
    static CROSS_CHAIN_DATA_BYTES = 160;
    // eslint-disable-next-line max-params
    constructor(address, auctionDetails, whitelist, hashLockInfo, dstChainId, dstToken, srcSafetyDeposit, dstSafetyDeposit, timeLocks, dstAddressFirstPart = address_complement_js_1.AddressComplement.ZERO, extra) {
        (0, assert_1.default)(srcSafetyDeposit <= byte_utils_1.UINT_128_MAX, 'srcSafetyDeposit exceeds max');
        (0, assert_1.default)(dstSafetyDeposit <= byte_utils_1.UINT_128_MAX, 'dstSafetyDeposit exceeds max');
        super(address.inner, auctionDetails, whitelist, fusion_sdk_1.SurplusParams.NO_FEE, extra);
        this.hashLockInfo = hashLockInfo;
        this.dstChainId = dstChainId;
        this.dstToken = dstToken;
        this.srcSafetyDeposit = srcSafetyDeposit;
        this.dstSafetyDeposit = dstSafetyDeposit;
        this.timeLocks = timeLocks;
        this.dstAddressFirstPart = dstAddressFirstPart;
        this.dstToken = dstToken.zeroAsNative();
    }
    /**
     * Decode extension from encoded bytes
     */
    static decode(bytes) {
        return EscrowExtension.fromExtension(fusion_sdk_1.Extension.decode(bytes));
    }
    /**
     * Create EscrowExtension from Extension
     */
    static fromExtension(extension) {
        const pi = extension.postInteraction;
        const crossChainData = EscrowExtension.decodeCrossChainData(byte_utils_1.BytesIter.HexString(pi).nextBytes(EscrowExtension.CROSS_CHAIN_DATA_BYTES, byte_utils_1.BytesIter.SIDE.Back));
        const iter = byte_utils_1.BytesIter.HexString(pi);
        const callback = iter.nextAddress();
        const integrator = iter.nextAddress();
        const protocol = iter.nextAddress();
        const restBytes = (iter.rest().length - 2) / 2;
        const feeAndWhitelistLength = restBytes - EscrowExtension.CROSS_CHAIN_DATA_BYTES;
        const feeAndWhitelist = iter.nextBytes(feeAndWhitelistLength);
        const postInteractionForFusion = new byte_utils_1.BytesBuilder()
            .addAddress(callback)
            .addUint8(0n) // flags
            .addAddress(integrator)
            .addAddress(protocol)
            .addBytes(feeAndWhitelist)
            .addBytes('0x' + '0'.repeat(66)) // surplus
            .asHex();
        const fusionExt = fusion_sdk_1.FusionExtension.fromExtension(new fusion_sdk_1.Extension({
            ...extension,
            postInteraction: postInteractionForFusion
        }));
        const complement = extension.customData === fusion_sdk_1.ZX
            ? address_complement_js_1.AddressComplement.ZERO
            : new address_complement_js_1.AddressComplement(BigInt(extension.customData));
        return new EscrowExtension(index_js_4.EvmAddress.fromString(fusionExt.address.toString()), index_js_1.AuctionDetails.fromBase(fusionExt.auctionDetails), fusionExt.whitelist, crossChainData.hashLock, crossChainData.dstChainId, crossChainData.dstToken, crossChainData.srcSafetyDeposit, crossChainData.dstSafetyDeposit, crossChainData.timeLocks, complement, fusionExt.extra);
    }
    static decodeCrossChainData(bytes) {
        const [hashLock, dstChainId, dstToken, safetyDeposit, timeLocks] = coder_js_1.coder.decode(EscrowExtension.CROSS_CHAIN_DATA_TYPES, bytes);
        const safetyDepositBN = new byte_utils_1.BN(safetyDeposit);
        return {
            hashLock: index_js_2.HashLock.fromString(hashLock),
            dstChainId: Number(dstChainId),
            dstToken: (0, index_js_4.createAddress)(dstToken.toString(), Number(dstChainId)),
            dstSafetyDeposit: safetyDepositBN.getMask(new byte_utils_1.BitMask(0n, 128n))
                .value,
            srcSafetyDeposit: safetyDepositBN.getMask(new byte_utils_1.BitMask(128n, 256n))
                .value,
            timeLocks: index_js_3.TimeLocks.fromBigInt(timeLocks)
        };
    }
    /**
     * Build extension
     */
    build() {
        const baseExt = super.build();
        const iter = byte_utils_1.BytesIter.HexString(baseExt.postInteraction);
        const extensionAddr = iter.nextAddress();
        const flags = new byte_utils_1.BN(BigInt(iter.nextUint8()));
        const integrator = iter.nextAddress();
        const protocol = iter.nextAddress();
        // Skip custom receiver if present
        if (flags.getBit(0n)) {
            iter.nextAddress();
        }
        const restBytes = (iter.rest().length - 2) / 2;
        const feeAndWhitelistLength = restBytes - 33; // surplus bytes
        const feeAndWhitelist = iter.nextBytes(feeAndWhitelistLength);
        const postInteraction = new byte_utils_1.BytesBuilder()
            .addAddress(extensionAddr)
            .addAddress(integrator)
            .addAddress(protocol)
            .addBytes(feeAndWhitelist)
            .addBytes(this.encodeCrossChainData())
            .asHex();
        return new fusion_sdk_1.Extension({
            ...baseExt,
            postInteraction,
            customData: this.encodeCustomData()
        });
    }
    encodeCrossChainData() {
        const packedSafetyDeposit = (this.srcSafetyDeposit << 128n) | this.dstSafetyDeposit;
        return coder_js_1.coder.encode(EscrowExtension.CROSS_CHAIN_DATA_TYPES, [
            this.hashLockInfo.toString(),
            this.dstChainId,
            this.dstToken.nativeAsZero().toHex(),
            packedSafetyDeposit,
            this.timeLocks.build()
        ]);
    }
    encodeCustomData() {
        if (!this.dstAddressFirstPart || this.dstAddressFirstPart.isZero()) {
            return fusion_sdk_1.ZX;
        }
        return this.dstAddressFirstPart.asHex();
    }
}
exports.EscrowExtension = EscrowExtension;
//# sourceMappingURL=escrow-extension.js.map