import { ImmutableFees } from '../immutables-fees/index.js';
/**
 * Complement data for destination chain immutables.
 * Contains the additional fields needed to construct dst escrow.
 */
export class DstImmutablesComplement {
    maker;
    amount;
    token;
    taker;
    safetyDeposit;
    chainId;
    fees;
    static Web3Type = `tuple(${[
        'address maker',
        'uint256 amount',
        'address token',
        'uint256 safetyDeposit',
        'uint256 chainId',
        'bytes parameters'
    ]})`;
    constructor(maker, amount, token, taker, safetyDeposit, chainId, fees) {
        this.maker = maker;
        this.amount = amount;
        this.token = token;
        this.taker = taker;
        this.safetyDeposit = safetyDeposit;
        this.chainId = chainId;
        this.fees = fees;
    }
    static new({ maker, amount, token, taker, safetyDeposit, chainId, fees = ImmutableFees.ZERO }) {
        return new DstImmutablesComplement(maker, amount, token, taker, safetyDeposit, chainId, fees);
    }
    toJSON() {
        return {
            maker: this.maker.toString(),
            amount: this.amount.toString(),
            token: this.token.toString(),
            safetyDeposit: this.safetyDeposit.toString(),
            chainId: this.chainId.toString(),
            fees: this.fees.encode()
        };
    }
}
//# sourceMappingURL=dst-immutables-complement.js.map