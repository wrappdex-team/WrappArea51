"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const bytes_iter_1 = require("./bytes-iter");
describe('BytesIter', () => {
    it('should iterate as BigInt ', () => {
        const iter = bytes_iter_1.BytesIter.BigInt('0xdeadbeef');
        expect(iter.nextByte()).toEqual(BigInt(0xde));
        expect(iter.nextByte()).toEqual(BigInt(0xad));
        expect(iter.nextBytes(2)).toEqual(BigInt(0xbeef));
    });
    it('should iterate as String ', () => {
        const iter = bytes_iter_1.BytesIter.HexString('0xdeadbeef');
        expect(iter.nextByte()).toEqual('0xde');
        expect(iter.nextByte()).toEqual('0xad');
        expect(iter.nextBytes(2)).toEqual('0xbeef');
    });
    it('should iterate in reverse ', () => {
        const iter = bytes_iter_1.BytesIter.HexString('0xdeadbeef');
        expect(iter.nextByte(bytes_iter_1.BytesIter.SIDE.Back)).toEqual('0xef');
        expect(iter.nextByte(bytes_iter_1.BytesIter.SIDE.Back)).toEqual('0xbe');
        expect(iter.nextBytes(2, bytes_iter_1.BytesIter.SIDE.Back)).toEqual('0xdead');
    });
});
//# sourceMappingURL=bytes-iter.spec.js.map