"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
Object.defineProperty(exports, "randBigInt", {
    enumerable: true,
    get: function() {
        return randBigInt;
    }
});
var _ethers = require("ethers");
function randBigInt(max) {
    var bytesCount = 0;
    max = BigInt(max) + 1n;
    var rest = max;
    while(rest){
        rest = rest >> 8n;
        bytesCount += 1;
    }
    var bytes = (0, _ethers.randomBytes)(bytesCount);
    var val = bytes.reduce(function(acc, val, i) {
        return acc + (BigInt(val) << BigInt(i * 8));
    }, 0n);
    return val % max;
}
