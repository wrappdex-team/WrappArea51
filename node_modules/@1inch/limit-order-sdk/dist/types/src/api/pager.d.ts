/**
 * Cursor-based pagination helper for API requests
 */
export declare class CursorPager {
    readonly limit: number;
    readonly cursor?: string;
    constructor({ limit, cursor }?: {
        limit?: number;
        cursor?: string;
    });
}
