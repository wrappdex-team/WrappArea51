import { LimitOrderWithFee, MakerTraits, OrderInfoData, FeeTakerExt, Interaction } from '../limit-order/index.js';
import { ApiConfig } from '../api/index.js';
export declare class Sdk {
    private readonly api;
    constructor(apiConfig: ApiConfig);
    /**
     * Create LimitOrder with an extension params from API
     *
     * @returns LimitOrderWithFee to sign and submit to relayer
     */
    createOrder(orderInfo: OrderInfoData, makerTraits?: MakerTraits, extra?: {
        makerPermit?: Interaction;
        integratorFee?: FeeTakerExt.IntegratorFee;
    }): Promise<LimitOrderWithFee>;
    submitOrder(order: LimitOrderWithFee, signature: string): Promise<void>;
}
