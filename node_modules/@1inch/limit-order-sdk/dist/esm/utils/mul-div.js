export var Rounding = /*#__PURE__*/ function(Rounding) {
    Rounding[Rounding["Ceil"] = 0] = "Ceil";
    Rounding[Rounding["Floor"] = 1] = "Floor";
    return Rounding;
}({});
// todo: move to more appropriate place
export function mulDiv(a, b, x) {
    var rounding = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : 1;
    var res = a * b / x;
    if (rounding === 0 && a * b % x > 0) {
        return res + 1n;
    }
    return res;
}
