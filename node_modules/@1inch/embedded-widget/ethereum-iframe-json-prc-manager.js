"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.EthereumIframeJsonRpcManager = void 0;
class EthereumIframeJsonRpcManager {
    constructor(iframe, ethereumProvider) {
        this.iframe = iframe;
        this.ethereumProvider = ethereumProvider;
        this.onIframeLoadCallback = null;
        this.isIframeLoaded = false;
        this.eventsListener = event => {
            if (event.data.jsonrpc === '2.0') {
                this.processRequest(event.data);
            }
        };
        window.addEventListener('message', this.eventsListener);
        this.iframe.addEventListener('load', () => {
            var _a;
            (_a = this.onIframeLoadCallback) === null || _a === void 0 ? void 0 : _a.call(this);
            this.isIframeLoaded = true;
        });
    }
    onIframeLoad(callback) {
        if (this.isIframeLoaded) {
            callback();
            return;
        }
        this.onIframeLoadCallback = callback;
    }
    destroy() {
        var _a;
        window.removeEventListener('message', this.eventsListener);
        (_a = this.iframe.parentElement) === null || _a === void 0 ? void 0 : _a.removeChild(this.iframe);
    }
    processRequest(request) {
        if (!this.ethereumProvider) {
            return Promise.resolve();
        }
        const request$ = request.method === 'enable'
            ? this.ethereumProvider.enable(request)
            : this.ethereumProvider.request(request);
        const contentWindow = this.iframe.contentWindow;
        return request$.then(result => {
            contentWindow.postMessage({
                jsonrpc: '2.0',
                id: request.id,
                result
            }, '*');
        }).catch(error => {
            contentWindow.postMessage({
                jsonrpc: '2.0',
                id: request.id,
                error
            }, '*');
        });
    }
}
exports.EthereumIframeJsonRpcManager = EthereumIframeJsonRpcManager;
//# sourceMappingURL=ethereum-iframe-json-prc-manager.js.map