import { ProviderConnector } from "src/connector/provider.connector";
import { AbiItem } from "src/model/abi.model";
import { ChainId } from "../limit-order-protocol-addresses.const";
export declare abstract class AbstractSmartcontractFacade<ABI_METHODS extends string> {
    readonly contractAddress: string;
    readonly chainId: ChainId | number;
    readonly providerConnector: ProviderConnector;
    abstract ABI: AbiItem[];
    constructor(contractAddress: string, chainId: ChainId | number, providerConnector: ProviderConnector);
    getContractCallData(methodName: ABI_METHODS, methodParams?: unknown[]): string;
}
