import { Nonce, PredicateTimestamp } from "./model/limit-order-protocol.model";
import { Series } from "./model/series-nonce-manager.model";
import { AbstractPredicateBuilder } from "./utils/abstract-predicate-builder";
import { SeriesNonceManagerFacade } from "./series-nonce-manager.facade";
/**
 * All methods is lambdas to preserve `this` context and allow DSL-like usage
 */
export declare class SeriesNonceManagerPredicateBuilder extends AbstractPredicateBuilder<SeriesNonceManagerFacade> {
    nonce: (series: Series, makerAddress: string) => string;
    nonceEquals: (series: Series, makerAddress: string, makerNonce: Nonce) => string;
    timestampBelowAndNonceEquals: (series: Series, timestamp: PredicateTimestamp, makerNonce: Nonce, makerAddress: string) => string;
}
